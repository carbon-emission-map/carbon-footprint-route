h1 {
    font-family: 'Roboto', sans-serif; /* 引入新字體 */
    color: #2c3e50; /* 新的字體顏色 */
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2); /* 添加文字陰影 */
}

button {
    transition: background-color 0.3s ease; /* 添加過渡效果 */
}

button:hover {
    background-color: #27ae60; /* 鼠標懸停時的顏色 */
}
